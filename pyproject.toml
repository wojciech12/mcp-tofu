[project]
name = "mcp-opentofu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic-ai>=0.2.3",
]

[dependency-groups]
dev = [
    "ruff>=0.11.9",
]
